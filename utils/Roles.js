
export const roles = [
  {
    ViewSubs: false,
    CreateSubs: false,
    DeleteSubs: false,
    ViewMovies: false,
    CreateMovies: false,
    DeleteMovies: false,
  },
];

const Roles = async (storeData, uid) => {
};
export default Roles;
